# Compatibility requirements for environments that do not install from pyproject.toml.
# Preferred development install:
#   python -m pip install -e .[dev]

fastapi>=0.111
httpx>=0.27
pydantic>=2.7
requests>=2.31
typer>=0.12
uvicorn[standard]>=0.30
watchdog>=4.0
pillow>=10.0
